module RoboCompSpeech{

	enum languageType {SPANISH, ENGLISH, MEXICAN};

	interface Speech{
		bool say(string text, bool overwrite);
		bool isBusy();
		void setLanguage(languageType type);
		void setVolume(double nvolume);
		void increaseVolume(double nvolume);
	};
};
